---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: instance
    image: debian:9
provisioner:
  name: ansible
  connection_options:
    ansible_ssh_user: evergreen
  lint:
    name: ansible-lint
verifier:
  name: inspec
  lint:
    name: rubocop
